spring:
   cache:
    type: redis
   redis:
    host: localhost
    port: 6379
   jpa:
    hibernate:
      ddl-auto: update
management:
  endpoints:
    web:
      exposure:
        include:
        - "*"
db:
  username: root
  password: root
  url: jdbc:mysql://localhost:3306/testDB